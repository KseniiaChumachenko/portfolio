import React from "react";
import styles from "../../styles/Intro.module.css";
import { Header } from "semantic-ui-react";

const Intro = () => (
  <div className={styles.container}>
    <Header as={"h1"}>INTRO</Header>
  </div>
);

export default Intro;
